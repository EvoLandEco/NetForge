# API reference

The API reference is generated from the Python source with `mkdocstrings`.

Use these pages when you want to inspect:

- the command line parser and stage orchestration
- the fit and generation pipeline
- diagnostics and report writers
- the transmission simulation module

These pages are generated from the source, so they reflect the current module signatures and docstrings.
